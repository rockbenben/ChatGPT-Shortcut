name: 🚀 Deploy website on push

on:
  push:
    branches:
      - speedup/data-retrieval
  workflow_dispatch:

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v7
        with:
          # 获取所有标记和分支的所有历史记录（updatetime 必须，否则每次都会变化）
          fetch-depth: 0
          # 如果你文档需要 Git 子模块，取消注释下一行
          # submodules: true

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile
      # 类型检查必须单独跑：Docusaurus 用 swc 剥离类型、构建期**不做**类型检查
      # （实测：带 TS2322 的代码 yarn build 仍以退出码 0 成功），类型错误会直接上线。
      # pretypecheck 还会顺带跑 scripts/checkClipboardFallback.mjs，
      # 否则那份自检在 CI 里永远不会执行。
      # 生成器在这里首次产出 5022 个薄壳，随后 prebuild 命中 .genstamp 快路径（~0.2s）。
      - name: Typecheck
        run: yarn typecheck
      - name: Build website
        run: yarn build

      # - name: Deploy to EdgeOne Pages
      #   run: npx edgeone pages deploy ./build --name aishort --token ${{ secrets.EDGEONE_API_TOKEN }}
      #   env:
      #     EDGEONE_API_TOKEN: ${{ secrets.EDGEONE_API_TOKEN }}

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          # 这是文档部署到的分支名称
          branch: gh-pages
          folder: build

      # - name: 🚀 Deploy to Azure Static Web Apps
      #   uses: Azure/static-web-apps-deploy@v1
      #   with:
      #     azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_GRAY_HILL_08B898100 }}
      #     repo_token: ${{ secrets.GITHUB_TOKEN }}
      #     action: "upload"
      #     ###### Repository/Build Configurations ######
      #     app_location: "/build" # 应用源代码路径
      #     api_location: "" # API 源代码路径（可选）
      #     output_location: "" # 构建输出目录
      #     skip_app_build: true # 跳过构建步骤，因为已经构建好了
      #     ###### End of Repository/Build Configurations ######

      # - name: 📂 Sync files
      #   uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      #   with:
      #     local-dir: build/
      #     server: ${{ secrets.ftp_host }}
      #     username: ${{ secrets.ftp_username }}
      #     password: ${{ secrets.ftp_password }}
      #     port: ${{ secrets.ftp_port }} # 建议更改默认的 21 端口
      #     timeout: 600000
