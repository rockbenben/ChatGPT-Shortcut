{
  "id": 9353,
  "title": "android atets 代码生成",
  "description": "帮忙生成这个文件的atest,要求：\n\t- 帮忙生成这个文件的atest,要求：(AI prompt)\n-  (1) 测试类的非static private变量和方法使用反射获取和设置需要的参数，注意注入时候需要传入类的实例，例如 ：field.set(instance, value)；\n-  (2) 测试类的static private变量使用反射获取和设置需要的参数，注意注入时候无需要传入类的实例，例如：field.set(null, value);\n-  (3) 测试类的static 方法的获取，需要使用 ExtendedMockitoRule 的方法mockito；使用ExtendedMockitoRule 方法需要包含的包名：\n     import com.android.dx.mockito.inline.extended.ExtendedMockito;\n    import com.android.modules.utils.testing.ExtendedMockitoRule;\n    import static com.android.dx.mockito.inline.extended.ExtendedMockito.*;\n       尽量使用ExtendedMockitoRule 的方式：例如：\n     @Rule\n    public final ExtendedMockitoRule mExtendedMockitoRule = new ExtendedMockitoRule.Builder(this)\n            .mockStatic(SystemProperties.class)\n            .mockStatic(ActivityManager.class)\n            .mockStatic(BatteryStatsService.class)\n            .build();\n-  (4) 单例类的优先使用注入instance 静态变量的方式进行mock，不要使用mockStatic的方式；\n-  (5) 如果有hander使用真实HandlerThread处理异步操作不要使用mock hander（符合要求）；\n-  (6) 真机上运行，不要使用Robolectric框架，使用@RunWith(AndroidJUnit4.class)或者@RunWith(MockitoJUnitRunner.class)框架皆可；\n-  (7) 假如强制要使用@RunWith(AndroidJUnit4.class)的话，注意不能直接mock static 方法，否则会报错；\n-  (8) 验证死亡监听、重连逻辑、方法调用等核心功能,代码覆盖率 > 70%;\n-  (9) 每个test函数记得都要使用 throws Exception 捕获异常;\n- (10) 每个test函数都再函数前附上解释的comment;\n- (11) 对应测试类中的xxx. getService的调用，因为是静态函数方法，所以需要使用static mockito去模拟，然后使用下面的方式mock:（注意务必使用doReturn/doNothing等方式，不要使用when(() -> ...).thenReturn(...)的方式)\n     static 方法：\n     doReturn(mMockIPlatformCompat)\n                .when(() -> ServiceManager.getService(Context.PLATFORM_COMPAT_SERVICE));\n     doNothing().when(() -> SystemProperties.set(anyString(), anyString()));\n     非 static方法：     \n     // 创建 mock SoundTriggerModule 并注入（通过反射）\n        mMockSTModule = mock(SoundTriggerModule.class);\n        doReturn(SoundTrigger.STATUS_OK).when(mMockSTModule).loadSoundModel(any(), any());\n        doReturn(SoundTrigger.STATUS_OK).when(mMockSTModule).unloadSoundModel(anyInt());\n        doReturn(SoundTrigger.STATUS_OK).when(mMockSTModule).startRecognition(anyInt(), any());\n        doReturn(SoundTrigger.STATUS_OK).when(mMockSTModule).stopRecognition(anyInt());\n        injectField(mAcdHelper, \"mSTModule\", mMockSTModule);\n- (12) 对应测试类中的public的调用的mock，必须使用下面的方式：\n          对于public 函数的mock 记住要使用下面的写法，不能使用 doReturn\n        // doReturn(false).when(() -> mMockAtms.isInSplitScreenWindowingMode());\n        when(mMockAtms.isInSplitScreenWindowingMode()).thenReturn(false);\t",
  "remark": "android atets 代码生成",
  "owner": "副热带",
  "upvotes": 2,
  "downvotes": 0,
  "upvoteDifference": 2,
  "updatedAt": "2026-04-13T20:38:54.819Z"
}
